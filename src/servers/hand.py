from __future__ import annotations


# region 主入口


class HandServer:
    """手部服务占位。

    职责边界：
    - 当前代码页只声明手部服务的归属模块。
    - 不实现夹爪、灵巧手、末端工具或触觉状态接口，避免在本体接口任务中扩展范围。

    设计思想：
    - 使用显式占位类让 `src.servers` 的三段结构完整。
    - 后续接入具体手部协议时，应先补齐数据结构，再实现可调用接口。

    生命周期：
    - 当前类不持有硬件连接、线程、协程或文件句柄。

    继承关系：
    - 不继承业务基类，避免在占位阶段引入额外约束。
    """


# endregion

