from __future__ import annotations


# region 主入口


class BaseServer:
    """底座服务占位。

    职责边界：
    - 当前代码页只声明底座服务的归属模块。
    - 不实现底盘运动、导航、急停或状态查询接口，避免在本体接口任务中混入未验证逻辑。

    设计思想：
    - 使用显式占位类让 `src.servers` 的三段结构完整。
    - 后续实现底盘 gRPC 或硬件适配时，应在本文件内继续细分数据结构与控制入口。

    生命周期：
    - 当前类不持有硬件连接、线程、协程或文件句柄。

    继承关系：
    - 不继承业务基类，避免在占位阶段引入额外约束。
    """


# endregion

